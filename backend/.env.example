# Sboboz Backend - copy to .env and fill in values
# Server
PORT=3000
NODE_ENV=development

# Database (Supabase/PostgreSQL) - add when Phase 1 DB is ready
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# Auth (JWT) - add when implementing auth
# JWT_SECRET=your-secret-min-32-chars
